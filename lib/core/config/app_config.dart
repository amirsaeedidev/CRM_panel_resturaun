class AppConfig {
  AppConfig._();

  static const String appName = 'CRM Panel';
  static const String appVersion = '1.0.0';
  static const bool enableLogging = true;
  static const Duration apiTimeout = Duration(seconds: 30);
}